/* 
    Class 
        Implement Interface
*/

interface Setting {
    them: boolean,
    font?: string,
    save: () => void,
}

/*class User31 implements Setting {
    constructor (public userName: string ,public theme: boolean ) {
        save: () => {
            console.log(`Saved`)
        }
    }
}
*/
